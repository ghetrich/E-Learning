<div class="btn-demo-notika sm-mg-t-30 mg-b-15">
	<div class="notika-btn-hd">
		<h4>Here another name</h4>
		<p>
			Combine sets of <code>.btn-group</code> into a
			<code>.btn-toolbar</code> for more complex components.
		</p>
	</div>

	<div class="btn-toolbar" role="toolbar">
		<div class="btn-group notika-tl-btn mg-b-10">
			<a
				href="/announcement/class/<%=classId%>/announcement-new"
				class="btn btn-default notika-gp-primary"
				>Make Announcement</a
			>
		</div>
	</div>
</div>
<%if(announcements.length > 0){%>

    <%for(var announcement of announcements){
       
        
        %>

        <!-- <%=announcement.read.includes(user._id)%> -->
<!-- Breadcomb area Start-->
<div class="breadcomb-area clickable">
	<div class="row">
		<div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
			<div class="breadcomb-list  <%if(!read(announcement.read,user._id)){%>unread<%}%>">
				<div class="row">
					<div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
						<a href="/announcement/class/<%=classId%>/announcement/<%=announcement._id%>"class="breadcomb-wp">
							<div class="breadcomb-icon">
								<!-- <i class="notika-icon notika-app"></i> -->
								<img
									src="<%=announcement.image%>"
									alt=""
									width="100px"
									height="100px"
								/>
							</div>
							<div class="breadcomb-ctn">
								<h2 class="txt-uppercase"><%=trunc(announcement.header, 65 , "...")%></h2>
								<p>
								<%=trunc(announcement.message, 110 , "...")%>									
								</p>
                                <br/>
                                <p class="notifyAt"><%=moment(announcement.createdAt).from()%></p>
							</div>
						</a>
					</div>
				</div>
			</div>
		</div>
	</div>
</div>
<!-- Breadcomb area End-->
<%}%>
<%}%>

